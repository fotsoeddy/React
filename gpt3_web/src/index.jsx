import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './index.css'

// This renders the App component into the root div from the HTML
ReactDOM.createRoot(document.getElementById('root')).render(<App />);
